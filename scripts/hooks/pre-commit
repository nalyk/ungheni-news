#!/usr/bin/env bash
set -euo pipefail

# Auto-regenerate AGENTS.md on commit and stage it if changed
bash scripts/update_agents.sh >/dev/null || true

if ! git diff --quiet -- AGENTS.md; then
  git add AGENTS.md
  echo "pre-commit: staged updated AGENTS.md"
fi

exit 0

